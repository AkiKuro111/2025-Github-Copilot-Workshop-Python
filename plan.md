# ポモドーロタイマーWebアプリ 段階的実装計画

## ステップ1: 最小構成のバックエンドとフロントエンド（完了）
- Flaskで/ルートにindex.htmlを返すだけのAPIを作成
- templates/index.html, static/css/style.css, static/js/timer.jsの雛形を作成
- シンプルな「ポモドーロタイマー」タイトルと空のタイマーUIを表示

## ステップ2: タイマーUIの実装（完了）
- index.htmlにタイマー表示（25:00）、開始・リセットボタンを配置
- style.cssで添付画像のようなモダンなデザインを適用
- timer.jsでボタン押下時のイベントハンドラを実装（まだカウントダウンは不要）

## ステップ3: タイマーカウントダウン機能（完了）
- timer.jsで25分のカウントダウンロジックを実装
- カウントダウン中はボタンの状態を切り替え
- 0になったら自動で停止

## ステップ4: 円形プログレスバーの実装（完了）
- SVGまたはCanvasで円形プログレスバーを描画
- タイマー進行に合わせてプログレスバーが動くようにする

## ステップ5: 進捗表示UIの実装（完了）
- 今日の進捗（完了数・集中時間）を表示するUIを追加
- ダミーデータで表示（API連携はまだ不要）

## ステップ6: 進捗データの保存・取得API
- Flaskで/api/progress GET/POSTエンドポイントを実装
- サーバー側で進捗データを一時的に保存（ファイルやメモリでOK）

## ステップ7: 進捗データのフロントエンド連携（完了）
- タイマー完了時にAPIへ進捗データをPOST
- ページ表示時にAPIから進捗データをGETし、UIに反映

## ステップ8: テスト実装
- バックエンドAPIのユニットテスト（pytest等）
- タイマーロジックのユニットテスト

## ステップ9: 拡張・リファクタリング
- コード整理、ロジックの分離
- 必要に応じてユーザー管理や設定画面、通知機能など拡張
