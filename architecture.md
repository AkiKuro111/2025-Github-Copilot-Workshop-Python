# ポモドーロタイマーWebアプリ アーキテクチャ案

## 1. 概要

本アプリはFlask（Python）をバックエンド、HTML/CSS/JavaScriptをフロントエンドとしたWebベースのポモドーロタイマーです。添付UIモックのようなモダンなデザインと、進捗管理機能を備えます。

## 2. 構成

- バックエンド: Flask
  - タイマー進捗データの保存・取得API
  - 必要に応じてセッション・ユーザー管理
- フロントエンド: HTML/CSS/JavaScript
  - タイマーUI（円形プログレスバー、開始・リセットボタン）
  - 進捗表示
  - APIとの通信（fetch/Ajax）

## 3. ディレクトリ・ファイル構成例

```
/プロジェクトルート/
├── app.py                # Flaskアプリ本体
├── templates/
│   └── index.html        # メイン画面テンプレート
├── static/
│   ├── css/
│   │   └── style.css     # スタイルシート
│   └── js/
│       └── timer.js      # タイマー制御用JS
├── tests/                # ユニットテスト
│   ├── test_routes.py
│   └── test_logic.py
└── architecture.md       # 本ドキュメント
```

## 4. 各層の役割

### Flask（バックエンド）
- `/` ルートで `index.html` を返す
- `/api/progress` などのAPIで進捗データの保存・取得
- ビジネスロジック（進捗計算・保存）は関数やクラスとして分離
- 設定や依存性はconfigで管理し、テスト時に差し替え可能

### HTML/CSS/JS（フロントエンド）
- タイマーのUI・ロジックはJavaScriptで実装（カウントダウン、円形プログレスバー描画、ボタン制御）
- 進捗データの取得・保存はAPI経由でFlaskと通信
- UIはモダンなデザイン（CSS、SVGやCanvasで円形プログレスバー）

## 5. ユニットテスト容易化のための工夫

- Flaskルートとロジックの分離（テストしやすい関数設計）
- 設定・依存性の注入（テスト用DBやモック利用）
- テストディレクトリの設置
- フロントエンドJSもロジック分離・テスト可能設計（Jest等利用）

## 6. 拡張性

- ユーザーごとの進捗管理（ログイン機能追加）
- 設定画面（作業/休憩時間のカスタマイズ）
- 通知機能（ブラウザ通知API）

---

このアーキテクチャにより、シンプルかつ拡張性・テスト容易性の高いWebアプリを実現します。
